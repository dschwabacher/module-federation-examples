{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @vue-in-react/* --parallel start",
    "build": "lerna run --scope @vue-in-react/* build",
    "serve": "lerna run --scope @vue-in-react/* --parallel serve",
    "clean": "lerna run --scope @vue-in-react/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "dependencies": {
    "vuera": "^0.2.7"
  }
}
